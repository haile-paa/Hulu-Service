# Your Go backend, same one the mobile app and bot use. Must be reachable
# over HTTPS from inside Telegram's WebView (not localhost) once deployed —
# see README for how to test this locally with ngrok.
VITE_BACKEND_URL=http://localhost:8080/api
